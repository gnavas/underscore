var House = function (color, rooms, sqFeet){
  this.color = color;
  this.rooms = rooms;
  this.sqFeet = sqFeet;
};

House.prototype.showFeet = function(){
  alert(this.sqFeet);
};



// newArr.push(array[0][key]);

 x.forEach(function(x){
    if (Array.isArray(x) === false){
      newArr.push(x);
    }else {
      x.forEach(function(x){

      });
    }


  });


